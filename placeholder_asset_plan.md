# 临时占位素材方案（V1 → AI 素材过渡）

> 纯规划文档，不包含代码。
> 原则：数据与表现分离，后续切 AI 素材只换纹理引用，不动逻辑。

---

## 一、棋盘格子

| 属性 | 值 |
|---|---|
| 尺寸 | 64×64 px（实际 cell，不含间距） |
| 间距 | 4px |
| 棋盘总尺寸 | 6×(64+4) = 408px 正方形 |
| 空cell样式 | 浅米色圆角矩形 `#F5EDE3`，圆角 8px，1px 边框 `#D4C5B2` |
| 有物品cell样式 | 同上底色，物品图标居中叠在上面 |
| 实现方式 | 单个 `Control` 节点 + `StyleBoxFlat`，不创建 36 个独立节点 |

## 二、物品占位（12 种）

### 设计规则

- 每个物品 = **纯色圆角方块 + 类型emoji/符号 + 等级角标**
- 三种链用三种色系区分
- 等级用角标数字（①②③④）或小星星数量表示

### 三链色板

| 链 | 底色 | 边框色 | 符号 |
|---|---|---|---|
| 面包线 | `#F4A460`（沙棕色） | `#D2894A` | 🍞 |
| 甜点线 | `#F8B0C2`（粉红色） | `#D48094` | 🎂 |
| 饮品线 | `#8B6F47`（咖啡棕） | `#6B4F2F` | ☕ |

### 等级递进

| Lv | 角标 | 额外视觉变化 |
|---|---|---|
| 1 | ① | 最朴素 |
| 2 | ② | 方块加 1px 发光边 |
| 3 | ③ | 方块加 2px 发光边 + 轻微阴影 |
| 4 | ④ | 方块加金色边框 `#FFD700` + 阴影 |

### 实现方式

- 一个 `ItemView.gd` 脚本，根据 `Item.type` 和 `Item.level` 动态设置颜色和角标
- 用 `StyleBoxFlat` + `Label` 组合，无需任何外部图片
- 后续切 AI 素材时：`ItemView` 改为加载 `res://assets/items/{type}_{level}.png`，其余逻辑不变

---

## 三、猫咪占位（3 只）

| 猫 | 占位色 | 占位形状 | 位置 |
|---|---|---|---|
| 面包猫 | 暖橙色 `#FF9F43` | 圆形 + 三角耳 + 😺 | 屏幕左上角 |
| 咖啡猫 | 深棕色 `#6B4226` | 圆形 + 三角耳 + 😸 | 屏幕左上角 |
| 工程猫 | 灰蓝色 `#7B8FA1` | 圆形 + 三角耳 + 😼 | 屏幕左上角 |

- 每只猫 = 一个 48×48 的圆形区域，用 `_draw()` 绘制简单猫脸
- 激活态：金色外圈光晕
- 未激活态：灰色半透明
- 切换时：简单的位置平移动画
- 后续切 AI 素材时：圆形区域改为 `TextureRect` 加载猫咪 sprite sheet

---

## 四、UI 占位元素

### 体力条
- 位置：屏幕顶部
- 样式：`ProgressBar` 或 `TextureProgressBar`，绿色渐变 `#7BC67E → #4A9C4E`
- 文字：`14/20 ⚡`

### 金币显示
- 位置：右上角
- 样式：金色文字 + 🪙 图标
- 字体：Godot 默认字体，字号 20

### 生成按钮
- 位置：棋盘下方
- 样式：大圆角按钮，背景 `#FF9F43`，文字"生成 (1⚡)"
- 冷却中：灰色 + 倒计时文字

### 自动合成开关
- 位置：棋盘右下角
- 样式：Toggle 开关，关闭=灰色，开启=绿色
- 标签："自动合成"

### 库存面板
- 位置：棋盘右侧或下方
- 样式：带滚动条的网格，每格 48×48
- 空位显示虚线边框
- 标题："📦 库存 (12/25)"

### 订单面板
- 位置：右侧
- 样式：3 个订单卡片纵向排列，每张卡片显示需求和奖励
- 完成时卡片闪烁绿色后消失

### 确认弹窗/Toast
- 样式：居中半透明背景 + 圆角白色卡片
- Toast：顶部弹出，2s 后自动消失（"库存已满！""收取成功！"等）

---

## 五、生成方式总结

| 素材类型 | V1 占位方式 | 切换 AI 素材时 |
|---|---|---|
| 棋盘格子 | `StyleBoxFlat` 代码绘制 | 换背景纹理 |
| 物品图标 | `StyleBoxFlat` + 文字符号，代码染色 | 加载 PNG，删掉染色逻辑 |
| 猫咪 | `_draw()` 圆形+emoji | 加载 sprite sheet |
| 体力条 | `ProgressBar` 内置控件 | 可换定制 bar 纹理 |
| 按钮 | `Button` + `StyleBoxFlat` | 可换定制按钮纹理 |
| 弹窗 | `Panel` + `StyleBoxFlat` | 可换定制弹窗背景 |
| 字体/图标 | 系统默认字体 + Unicode emoji | 可换定制字体 |

---

## 六、过渡到 B 方案（AI 素材）的最低工作量

当日后 AI 素材就绪时，只需做：

1. 将生成的 PNG 放入 `res://assets/items/`、`res://assets/cats/`、`res://assets/ui/`
2. 修改对应的 View 脚本：把 `_draw()` 或 `StyleBoxFlat` 改为 `TextureRect` 加载 PNG
3. 微调尺寸和位置（因为 PNG 尺寸与占位方块可能有差异）
4. 占位阶段的**所有游戏逻辑代码零改动**

这就是"数据与表现分离"的收益。
